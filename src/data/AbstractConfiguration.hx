package data;

class AbstractConfiguration 
{
	public static var tileSize;
	public static function getLevelWidth(level:Int):Int { return -1; }
	public static function getLevelHeight(level:Int):Int { return -1; }
}